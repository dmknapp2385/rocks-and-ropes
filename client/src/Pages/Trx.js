import React from "react";
import { Container } from 'react-bootstrap';

function Trx({setShowModal}) {
    return(
        <Container fluid>
            
        </Container>
    )
}

export default Trx;
